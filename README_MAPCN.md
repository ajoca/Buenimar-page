Este archivo era una guía temporal para integrar `mapcn`.

Se revirtió la integración y la página de cobertura volvió a usar el mapa SVG original.

Si necesitás recuperar la integración con `maplibre-gl` o intentar enlazar una versión remota de `mapcn`, avisame y lo preparo de nuevo.
