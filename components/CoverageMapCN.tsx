// Revert: este archivo ya no se usa. Exportamos directamente el `CoverageShapeMap` para mantener compatibilidad si se importa por error.

"use client";

import CoverageShapeMap from "./CoverageShapeMap";
export default CoverageShapeMap;
